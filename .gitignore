# Sem a barra final DE PROPÓSITO: `node_modules/` casa apenas diretórios, e um symlink
# chamado `node_modules` (comum ao trabalhar com git worktree, para não reinstalar) escapa
# da regra e acaba versionado — aconteceu em 03/09/2026 e o link, apontando para um caminho
# local, quebraria o checkout de qualquer outra máquina.
node_modules
test-results/
# A execução de 03/09/2026 gravou os artefatos por teste em test-results-0903/ (561 MB de trace,
# vídeo e screenshot). Mesma natureza de test-results/ — evidência reproduzível, não fonte.
# O que precisa ser lido está embutido no relatório HTML e no pacote .zip da execução.
test-results-0903/
playwright-report/
playwright/.cache/
playwright/.auth/
blob-report/

.env
.env.test
.env.local
blob-slices/
blob-todos/
qa-probe-*
tmp-cancel/
probe-*.mjs
limpeza.json
limpeza-alvos.json

.claude/
